# Бэклог: React Native приложение

Атомарные таски по фазам. Чеклисты — отмечай `[x]` по мере выполнения.
Оценки — грубые (соло-разработка, без учёта ревью).

## Фаза 0 — подготовка бэкенда

- [x] Вычистить DELETE-хвосты по AUDIT-LEGACY.md (контроллеры, сервисы,
      сущности + миграция, неиспользуемые контейнеры). **Готово 2026-07-06.**
- [ ] REWORK-хвосты по AUDIT-LEGACY.md, сознательно отложенные при чистке
      2026-07-06 (не «мёртвый код», а переименования с реальными
      побочными эффектами):
  - [ ] Сменить JWT Issuer/Audience "PolishTutor" → "Vass"
        (`appsettings.json`, `docker-compose.yml`). ⚠ Инвалидирует все
        выданные токены — активные пользователи (включая тёщу) вылетят и
        должны будут залогиниться заново один раз. Делать в момент, когда
        можно предупредить/помочь с ре-логином, не втихую.
  - [ ] Переименовать `TutorService` → `CompanionPromptService`; выкинуть
        польский `GetDefaultPrompt()` и неиспользуемые параметры
        (`mode`, `user`, `fullTranslation` — сейчас ничего не делают).
  - [ ] Переименовать `Prompts/tutor-system.txt` → `companion-system.txt`.
  - [ ] Переименовать Linux-пользователя `tutor` → `app` в Dockerfile
        (косметика, без риска).
  - [ ] Убрать `User.Level`/`User.NativeLang` из UI (`chat.js:21` всё ещё
        показывает уровень) — колонки в БД оставить.
  - [ ] Убрать чекбокс "полный перевод" (`UserSettings.FullTranslation`) из
        `settings.html` — колонку оставить.
- [ ] **Переписать системный промпт под компаньона** — главный содержательный
      пункт, влияет на «личность» Ольги. Сейчас промпт всё ещё каркасно
      «тьюторский» (проверить `Prompts/tutor-system.txt` целиком на предмет
      остатков языка/уровня/упражнений). Тон и сценарии — из презентации.
      Это отдельная содержательная задача, не рефакторинг.
- [ ] `/api/v1/` префикс + минимальный контракт для мобильного клиента
      (версионирование до первого релиза приложения).
- [ ] Упрощённый вход для пожилых: device-token / магическая ссылка от
      родственника вместо email+пароль.

## Фаза 1 — скелет приложения, паритет с вебом

- [ ] Инициализировать `mobile/` (Expo, TypeScript) в монорепо рядом с
      `VoiceAssistant.API/`.
- [ ] Настроить локальную сборку через WSL (см. [BUILD-WSL.md](./BUILD-WSL.md))
      и убедиться, что debug-APK ставится на физическое устройство.
- [ ] Auth-экран на существующем JWT.
- [ ] Экран чата-лога (вторичный — основной экран голосовой).
- [ ] Нативная аудио-сессия: full-duplex запись+воспроизведение
      (`AVAudioSession` / `AudioManager`).
- [ ] Silero VAD on-device (замена RMS-порога).
- [ ] Голосовой цикл с серверным TTS (стриминг PCM, эндпоинт уже есть —
      `/api/chat/tts_stream`).
- [ ] Порт shadow-capture логики (перебивание + «дослушивание» продолжения).
- [ ] Порт real-turn-taking логики (потолок паузы 7с, completeness-check,
      backchannel-фразы) — сейчас живёт в `frontend/js/yolo.js`.
- Критерий выхода: RN-приложение разговаривает не хуже веб-версии, без
  iOS-хаков (см. [README.md — раздел 0](./README.md#0-что-уже-доказано-веб-прототипом-этап-1)).

## Фаза 2 — нативные суперсилы

- [ ] Встроенный нейро-TTS: sherpa-onnx + `ru_RU-irina-medium` (бандл
      модели, синтез по предложениям, RMS-огибающая наружу для аватара).
- [ ] Фоновый режим: слушание при заблокированном экране (iOS audio mode /
      Android foreground service).
- [ ] Кнопки гарнитуры: PTT/перебивание.
- [ ] Экран «для пожилых»: одна большая кнопка, крупный статус, высокий
      контраст.
- Критерий выхода: телефон в кармане + наушники = полноценный компаньон.

## Фаза 3 — аватар

- [ ] Интеграция Rive (`rive-react-native`), 4 состояния (idle/listening/
      thinking/speaking).
- [ ] Рот по амплитуде TTS (RMS-огибающая → state machine input).
- [ ] (Опционально) тег эмоции из LLM-ответа (`[warm]`, `[cheerful]`) →
      выражение лица.

## Фаза 4 — память полного цикла

- [ ] pgvector-расширение к Postgres.
- [ ] Таблица `MemoryFacts` + извлечение фактов side-call'ом (Gemini).
- [ ] RAG-подмешивание top-K фактов в системный промпт.
- [ ] Скользящее резюме сессии (среднесрочная память, `ChatSession.Summary`).
- [ ] Напоминания: «напомни мне …» → серверная джоба → push (FCM/APNs) + голос.

## Фаза 5 — контент-компаньон

- [ ] Intent-роутер (классификация side-call'ом).
- [ ] YouTube: поиск (Data API v3) + `react-native-youtube-iframe` +
      голосовое управление.
- [ ] Новости: сводка через grounding, чтение по предложениям.
- [ ] Сериалы: TMDB-поиск, голосовые карточки.
- [ ] Рецепты: пошаговый режим с паузами и командами навигации.

## Фаза 6 — пилот

- [ ] Онбординг силами родственника (настройка профиля, память, контакты).
- [ ] Пилот с первыми пользователями.
- [ ] Метрики: удержание диалога, количество реплик/день, использование
      сценариев.
- [ ] Решение об облачных EAS-сборках / публикации в сторы (не раньше этого
      пункта — до сих пор только локальные сборки).
